/**
 * Checks compatibility between ionrift-resonance and active character sheets.
 * @param {ReportBuilder} builder
 */
export async function checkSheetCompatibility(builder) {
    // Stub: no active compat checks yet.
    builder.addResult("Ionrift Resonance", "Sheet Compatibility", "PASS", "No compatibility issues detected.");
}
